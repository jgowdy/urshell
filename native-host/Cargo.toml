[package]
name = "urshell-host"
version = "1.0.0"
edition = "2024"
description = "Native messaging host for URShell Chrome extension"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "5.0"

[profile.release]
strip = true
lto = true
opt-level = "z"
